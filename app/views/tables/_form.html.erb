<%= form_for(@table) do |f| %>
  <% if @table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@table.errors.count, "error") %> prohibited this table from being saved:</h2>

      <ul>
      <% @table.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :table %><br />
    <%= f.text_field :table %>
  </div>
  <div class="field">
    <%= f.label :table_url %><br />
    <%= f.text_field :table_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

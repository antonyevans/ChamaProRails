<h1>Listing tables</h1>

<table>
  <tr>
    <th>Table</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tables.each do |table| %>
  <tr>
    <td><%= link_to table.table, :controller => table.table_url %></td>
    <td><%= link_to 'Show', table %></td>
    <td><%= link_to 'Edit', edit_table_path(table) %></td>
    <td><%= link_to 'Delete', table, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Table', new_table_path %> <br>

<br />
<table>
	<tr><td>
<table>
  <tr>
    <th>Admins</th>
  </tr>

<% @admins.each do |admin| %>
  <tr>
    <td><%= admin.email %></td>
  </tr>
<% end %>
</table>
</td>
<td>
<table>
  <tr>
    <th>Managers</th>
  </tr>

<% @managers.each do |manager| %>
  <tr>
    <td><%= manager.email %></td>
  </tr>
<% end %>
</table>
</td>
 </tr>
</table>
